# FastAPI CRUD API - Управління Користувачами

Простий REST API для управління користувачами з використанням FastAPI, SQLAlchemy та SQLite.

## 📋 Вимоги

- Python 3.10+
- pip

## 🚀 Встановлення та Запуск

### 1. Встановіть залежності:

```bash
pip install -r requirements.txt
```

### 2. Запустіть сервер:

```bash
uvicorn main:app --reload
```

Сервер буде доступний за адресою: `http://127.0.0.1:8000`

### 3. Відкрийте документацію API:

- **Swagger UI**: http://127.0.0.1:8000/docs
- **ReDoc**: http://127.0.0.1:8000/redoc

## 📁 Структура Проєкту

```
fastapi_crud/
│
├── main.py          # Головний файл з ендпоінтами
├── models.py        # SQLAlchemy моделі
├── schemas.py       # Pydantic схеми
├── database.py      # Налаштування бази даних
├── requirements.txt # Залежності
└── users.db         # SQLite база даних (створюється автоматично)
```

## 🔌 API Ендпоінти

### Створити користувача
- **POST** `/users`
- **Body**:
```json
{
  "name": "Іван Петренко",
  "email": "ivan@example.com",
  "is_active": true
}
```
- **Response**: 201 Created

### Отримати всіх користувачів
- **GET** `/users`
- **Query параметри**: 
  - `skip` (опціонально): кількість записів для пропуску
  - `limit` (опціонально): максимальна кількість записів
- **Response**: 200 OK

### Отримати користувача за ID
- **GET** `/users/{user_id}`
- **Response**: 200 OK або 404 Not Found

### Оновити користувача
- **PUT** `/users/{user_id}`
- **Body** (всі поля опціональні):
```json
{
  "name": "Нове Ім'я",
  "email": "new@example.com",
  "is_active": false
}
```
- **Response**: 200 OK або 404 Not Found

### Видалити користувача
- **DELETE** `/users/{user_id}`
- **Response**: 204 No Content або 404 Not Found

## 🧪 Приклади Використання (curl)

### Створити користувача:
```bash
curl -X POST "http://127.0.0.1:8000/users" \
  -H "Content-Type: application/json" \
  -d '{"name":"Іван","email":"ivan@test.com","is_active":true}'
```

### Отримати всіх користувачів:
```bash
curl -X GET "http://127.0.0.1:8000/users"
```

### Отримати користувача за ID:
```bash
curl -X GET "http://127.0.0.1:8000/users/1"
```

### Оновити користувача:
```bash
curl -X PUT "http://127.0.0.1:8000/users/1" \
  -H "Content-Type: application/json" \
  -d '{"name":"Петро"}'
```

### Видалити користувача:
```bash
curl -X DELETE "http://127.0.0.1:8000/users/1"
```

## ✨ Особливості

- ✅ Валідація email через Pydantic
- ✅ Унікальність email у базі даних
- ✅ Коректні HTTP статус коди (200, 201, 204, 404, 400)
- ✅ Автоматична документація API
- ✅ Підтримка часткового оновлення (PATCH-like PUT)
- ✅ Dependency Injection для сесій БД

## 📝 Примітки

- База даних `users.db` створюється автоматично при першому запуску
- Для production середовища рекомендується використовувати PostgreSQL замість SQLite
- Для міграцій бази даних можна використати Alembic
