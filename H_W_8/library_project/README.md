# 📚 Django Бібліотека

Повнофункціональний Django-застосунок для керування авторами та книгами.

## 📋 Опис проєкту

Це веб-застосунок для керування бібліотекою, який включає:
- **Моделі**: Author (Автор) та Book (Книга) з відношенням ForeignKey
- **Django Admin**: повна адміністративна панель
- **Класові представлення (CBV)**: ListView для різних сторінок
- **Шаблони з наслідуванням**: base.html з дочірніми шаблонами
- **Анотації запитів**: підрахунок книг через Count()
- **Фільтрація**: книги за автором
- **Адаптивний дизайн**: CSS з градієнтами та сучасним оформленням

## 🚀 Встановлення та запуск

### 1. Встановіть Django

```bash
# Створіть віртуальне середовище (рекомендовано)
python -m venv venv

# Активуйте віртуальне середовище
# На Windows:
venv\Scripts\activate
# На macOS/Linux:
source venv/bin/activate

# Встановіть Django
pip install django
```

### 2. Застосуйте міграції

```bash
cd library_project
python manage.py makemigrations
python manage.py migrate
```

### 3. Створіть суперкористувача для адмін-панелі

```bash
python manage.py createsuperuser
```

Введіть:
- Ім'я користувача
- Email (необов'язково)
- Пароль (двічі)

### 4. Завантажте тестові дані

```bash
python manage.py loaddata library/fixtures/initial_data.json
```

Це додасть:
- 5 авторів (Тарас Шевченко, Леся Українка, Іван Франко, Ліна Костенко, Григорій Сковорода)
- 10 книг української класичної літератури

### 5. Запустіть сервер

```bash
python manage.py runserver
```

Сервер запуститься на `http://127.0.0.1:8000/`

##  URL-адреси

- **Головна/Автори**: http://127.0.0.1:8000/authors/
- **Книги**: http://127.0.0.1:8000/books/
- **Книги автора**: http://127.0.0.1:8000/authors/1/books/ (де 1 - ID автора)
- **Адмін-панель**: http://127.0.0.1:8000/admin/

##  Структура проєкту

```
library_project/
├── library_project/          # Головний проєкт
│   ├── __init__.py
│   ├── settings.py          # Налаштування Django
│   ├── urls.py              # Головні URL
│   ├── wsgi.py
│   └── asgi.py
├── library/                  # Застосунок бібліотеки
│   ├── migrations/          # Міграції БД
│   ├── templates/library/   # Шаблони
│   │   ├── authors.html
│   │   ├── books.html
│   │   └── author_books.html
│   ├── fixtures/            # Тестові дані
│   │   └── initial_data.json
│   ├── __init__.py
│   ├── admin.py            # Адмін-панель
│   ├── apps.py
│   ├── models.py           # Моделі (Author, Book)
│   ├── urls.py             # URL застосунку
│   └── views.py            # Класові представлення
├── templates/               # Глобальні шаблони
│   └── base.html           # Базовий шаблон
├── static/                  # Статичні файли
│   └── css/
│       └── style.css       # CSS стилі
├── manage.py               # Утиліта Django
└── db.sqlite3              # База даних (після міграцій)
```

## Основні функції

### Моделі

**Author (Автор)**:
- `name` — ім'я автора (CharField)
- `email` — email (EmailField, необов'язкове)
- `bio` — біографія (TextField, необов'язкове)

**Book (Книга)**:
- `title` — назва (CharField)
- `description` — опис (TextField)
- `published_date` — дата публікації (DateField)
- `author` — зв'язок з автором (ForeignKey)

### Представлення (Views)

**AuthorListView**:
- Показує всіх авторів
- Використовує `Count()` для підрахунку книг
- URL: `/authors/`

**BookListView**:
- Показує всі книги з інформацією про авторів
- Використовує `select_related()` для оптимізації
- URL: `/books/`

**AuthorBooksView**:
- Фільтрує книги за конкретним автором
- Приймає `author_id` як параметр
- URL: `/authors/<author_id>/books/`

### Адмін-панель

- Реєстрація моделей Author та Book
- Пошук, фільтри, сортування
- Відображення кількості книг для кожного автора
- Автодоповнення при виборі автора для книги

## 💡 Додаткові можливості

- ✅ Сортування книг (нові першими)
- ✅ Адаптивний дизайн
- ✅ Градієнтні кольори та сучасний UI
- ✅ Breadcrumbs (хлібні крихти)
- ✅ Порожні стани з посиланнями на додавання
- ✅ Українська локалізація

## 🎨 Технології

- Django 4.2+ (стабільна версія)
- SQLite (база даних)
- Class-Based Views (CBV)
- Django ORM з анотаціями
- HTML5 + CSS3
- Template inheritance (наслідування шаблонів)

## 📝 Як додавати дані

### Через адмін-панель:

1. Перейдіть на http://127.0.0.1:8000/admin/
2. Увійдіть під створеним суперкористувачем
3. Додайте авторів через розділ "Автори"
4. Додайте книги через розділ "Книги"

### Через Django shell:

```python
python manage.py shell

from library.models import Author, Book
from datetime import date

# Створення автора
author = Author.objects.create(
    name="Ім'я Автора",
    email="email@example.com",
    bio="Біографія автора"
)

# Створення книги
book = Book.objects.create(
    title="Назва книги",
    description="Опис книги",
    published_date=date(2024, 1, 1),
    author=author
)
```

## 🔧 Корисні команди

```bash
# Створення нових міграцій
python manage.py makemigrations

# Застосування міграцій
python manage.py migrate

# Запуск сервера
python manage.py runserver

# Вхід в Django shell
python manage.py shell

# Створення суперкористувача
python manage.py createsuperuser

# Завантаження fixtures
python manage.py loaddata library/fixtures/initial_data.json



## 📚 Тестові дані

Проєкт містить fixtures з 5 авторами та 10 книгами української класичної літератури:

**Автори**:
- Тарас Шевченко
- Леся Українка
- Іван Франко
- Ліна Костенко
- Григорій Сковорода

**Книги**: Кобзар, Гайдамаки, Лісова пісня, Захар Беркут, Маруся Чурай та інші.

## 🎓 Виконані вимоги

- ✅ Моделі з відношеннями (Author → Book через ForeignKey)
- ✅ Django Admin з повною функціональністю
- ✅ Класові представлення (ListView)
- ✅ Шаблони з наслідуванням (base.html)
- ✅ Анотації запитів (Count для підрахунку книг)
- ✅ Маршрутизація URL з параметрами
- ✅ Фільтрація queryset за автором
- ✅ Базове CSS-оформлення
- ✅ Тестові дані через fixtures



