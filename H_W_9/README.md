# Домашнє завдання: Асинхронне програмування в Python

## 📋 Зміст

1. [Завдання 1: HTTP запити (async vs sync vs multiprocessing)](#завдання-1)
2. [Завдання 2: FastAPI blocking vs non-blocking](#завдання-2)
3. [Інсталяція](#інсталяція)
4. [Запуск](#запуск)
5. [Очікувані результати](#очікувані-результати)

---

## Завдання 1: HTTP запити

### Файли:
- `async_vs_sync_requests.py` - Асинхронна vs Синхронна реалізація
- `multiprocessing_requests.py` - Реалізація через multiprocessing

### Що робить:
- Виконує 5 HTTP GET запитів до `https://httpbin.org/delay/2`
- Порівнює 3 підходи:
  - ✅ **Асинхронний** (asyncio + aiohttp)
  - ❌ **Синхронний** (requests)
  - ⚙️ **Multiprocessing** (окремі процеси)

### Очікувані результати:

```
АСИНХРОННИЙ:  ~2-3 секунди  (всі запити паралельно)
СИНХРОННИЙ:   ~10 секунд    (запити послідовно: 5 × 2с)
MULTIPROCESSING: ~2-4 секунди (паралельно, але з накладними витратами)
```

---

## Завдання 2: FastAPI blocking vs non-blocking

### Файли:
- `server.py` - FastAPI сервер з двома ендпоінтами
- `client.py` - Клієнт для тестування

### Ендпоінти:

#### 🔴 `/blocking` (БЛОКУЮЧИЙ)
- Використовує `time.sleep(2)`
- **БЛОКУЄ** потік виконання
- Запити обробляються **ПОСЛІДОВНО**

#### 🟢 `/non-blocking` (НЕ-БЛОКУЮЧИЙ)
- Використовує `await asyncio.sleep(2)`
- **НЕ БЛОКУЄ** event loop
- Запити обробляються **ПАРАЛЕЛЬНО**

### Очікувані результати:

При 5 паралельних запитах:
```
/non-blocking:  ~2 секунди   (всі запити одночасно)
/blocking:      ~10 секунд   (запити по черзі)

Різниця: ~5x повільніше!
```

---

## 🔧 Інсталяція

### 1. Створіть віртуальне середовище (рекомендовано):

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 2. Встановіть залежності:

```bash
pip install aiohttp requests fastapi uvicorn
```

Або через requirements.txt:

```bash
pip install -r requirements.txt
```

---

## 🚀 Запуск

### Завдання 1: HTTP запити

#### Варіант 1: Async vs Sync
```bash
python async_vs_sync_requests.py
```

Вивід:
```
====================================
АСИНХРОННА РЕАЛІЗАЦІЯ (5 запитів)
====================================
  Async запит #1 завершено за 2.12s
  Async запит #2 завершено за 2.13s
  ...
⏱️  Загальний час: 2.15 секунд

====================================
СИНХРОННА РЕАЛІЗАЦІЯ (5 запитів)
====================================
  Sync запит #1 завершено за 2.08s
  Sync запит #2 завершено за 2.09s
  ...
⏱️  Загальний час: 10.42 секунд

💡 Синхронна версія повільніша в 4.84 разів
```

#### Варіант 2: Multiprocessing
```bash
python multiprocessing_requests.py
```

Вивід:
```
====================================
MULTIPROCESSING РЕАЛІЗАЦІЯ (5 запитів)
====================================
Головний процес PID: 12345
  Process #1 (PID: 12346) завершено за 2.11s
  Process #2 (PID: 12347) завершено за 2.12s
  ...
⏱️  Загальний час: 2.45 секунд
```

---

### Завдання 2: FastAPI Server

#### Крок 1: Запустіть сервер

У **першому** терміналі:
```bash
python server.py
```

Вивід:
```
====================================
🚀 ЗАПУСК FASTAPI СЕРВЕРА
====================================

📌 Доступні ендпоінти:
   • http://localhost:8000/blocking     - блокуючий
   • http://localhost:8000/non-blocking - не-блокуючий
   • http://localhost:8000/stats        - статистика

INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

#### Крок 2: Запустіть клієнт

У **другому** терміналі:
```bash
python client.py
```

Вивід:
```
====================================
🚀 КЛІЄНТСЬКЕ ТЕСТУВАННЯ
====================================

🔴 ТЕСТУВАННЯ /BLOCKING (5 паралельних запитів)
🔴 Відправка запиту #1 до /blocking
🔴 Відправка запиту #2 до /blocking
...
⏱️  Загальний час: 10.156 секунд

🟢 ТЕСТУВАННЯ /NON-BLOCKING (5 паралельних запитів)
🟢 Відправка запиту #1 до /non-blocking
🟢 Відправка запиту #2 до /non-blocking
...
⏱️  Загальний час: 2.089 секунд

💡 Blocking повільніший у 4.86 разів
```

---

## 📊 Очікувані результати

### Завдання 1: HTTP Запити

| Метод | Час | Пояснення |
|-------|-----|-----------|
| **Асинхронний** | ~2с | ✅ Найшвидший - всі запити паралельно в одному потоці |
| **Multiprocessing** | ~2-3с | ⚙️ Швидкий, але накладні витрати на створення процесів |
| **Синхронний** | ~10с | ❌ Найповільніший - запити послідовно |

**Висновок:** Для I/O операцій використовуйте asyncio!

---

### Завдання 2: FastAPI Endpoints

| Ендпоінт | Час (5 запитів) | Поведінка |
|----------|-----------------|-----------|
| **/non-blocking** | ~2с | ✅ Всі запити обробляються одночасно |
| **/blocking** | ~10с | ❌ Запити обробляються послідовно |

**Різниця:** ~5x повільніше!

---

## 🔍 Технічне пояснення

### Чому non-blocking швидший?

#### 🟢 NON-BLOCKING (`await asyncio.sleep`)
```python
async def non_blocking():
    await asyncio.sleep(2)  # ✅ Повертає управління event loop
```

**Що відбувається:**
1. Event loop отримує 5 запитів
2. Кожен запит викликає `await asyncio.sleep(2)`
3. `await` звільняє event loop → може обробляти інші запити
4. Всі 5 запитів "сплять" **одночасно**
5. Час = ~2 секунди

**Діаграма:**
```
Request 1: |--sleep(2)--|
Request 2: |--sleep(2)--|  ← Паралельно
Request 3: |--sleep(2)--|
Request 4: |--sleep(2)--|
Request 5: |--sleep(2)--|
Total:     |----2s-----|
```

---

#### 🔴 BLOCKING (`time.sleep`)
```python
def blocking():
    time.sleep(2)  # ❌ БЛОКУЄ весь потік
```

**Що відбувається:**
1. Event loop отримує 5 запитів
2. Перший запит викликає `time.sleep(2)`
3. `time.sleep` **БЛОКУЄ** весь потік → event loop зупиняється
4. Інші запити **ЧЕКАЮТЬ** закінчення першого
5. Запити виконуються **ПОСЛІДОВНО**
6. Час = 5 × 2 = ~10 секунд

**Діаграма:**
```
Request 1: |--sleep(2)--|
Request 2:                |--sleep(2)--|  ← Послідовно
Request 3:                               |--sleep(2)--|
Request 4:                                              |--sleep(2)--|
Request 5:                                                             |--sleep(2)--|
Total:     |----10s----|
```

---

## 🎯 Ключові концепції

### 1️⃣ Event Loop
- **Що це:** Цикл, який обробляє асинхронні задачі
- **Як працює:** Переключається між задачами коли вони чекають I/O
- **Важливо:** НЕ блокуйте event loop синхронним кодом!

### 2️⃣ Блокування потоку
- `time.sleep()` → **БЛОКУЄ** потік
- `await asyncio.sleep()` → **НЕ БЛОКУЄ** event loop
- Blocked thread = thread нічого не може робити

### 3️⃣ Конкурентність vs Паралелізм
- **Конкурентність** (asyncio): один потік, переключення між задачами
- **Паралелізм** (multiprocessing): кілька процесів/потоків одночасно
- Для I/O: конкурентність = ефективніше!

---

## 💡 Коли що використовувати?

### ✅ Asyncio (найкраще для):
- HTTP запити
- Робота з базами даних
- Читання/запис файлів
- WebSockets
- **Будь-які I/O операції**

### ✅ Multiprocessing (найкраще для):
- CPU-інтенсивні обчислення
- Обробка великих обсягів даних
- Паралельні математичні розрахунки
- **CPU-bound задачі**

### ❌ Синхронний код (уникати для):
- Множинні I/O операції
- Сервери з високим навантаженням
- Довгі операції очікування

---

## 📝 Структура файлів

```
.
├── README.md                      # Ця інструкція
├── requirements.txt               # Залежності
│
├── async_vs_sync_requests.py     # Завдання 1: async vs sync
├── multiprocessing_requests.py   # Завдання 1: multiprocessing
│
├── server.py                     # Завдання 2: FastAPI сервер
└── client.py                     # Завдання 2: Клієнт для тестування
```

---

## 🐛 Troubleshooting

### Проблема: "Module not found"
```bash
pip install aiohttp requests fastapi uvicorn
```

### Проблема: "Connection refused" в client.py
1. Переконайтесь що `server.py` запущений
2. Перевірте що сервер слухає на `http://localhost:8000`
3. Перевірте файрвол

### Проблема: Повільні запити на Windows
- Це нормально через особливості multiprocessing на Windows
- Результати на Linux/Mac будуть кращі

---

## 📚 Додаткові ресурси

- [Python asyncio документація](https://docs.python.org/3/library/asyncio.html)
- [FastAPI документація](https://fastapi.tiangolo.com/)
- [aiohttp документація](https://docs.aiohttp.org/)
- [Real Python: Async IO](https://realpython.com/async-io-python/)

---

## ✨ Автор

Виконано як домашнє завдання з асинхронного програмування в Python

**Дата:** 2026

---

## 📄 Ліцензія

MIT License - використовуйте як завгодно для навчання!
